# Nanto plugin collision fixture

This behavior-free package deliberately reuses the filesystem fixture plugin identifier. Tests use it to prove that package-consumer builds reject catalog ownership collisions.
